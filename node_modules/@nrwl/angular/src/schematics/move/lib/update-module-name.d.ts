import { SchematicContext } from '@angular-devkit/schematics';
import { Observable } from 'rxjs';
import { Schema } from '../schema';
/**
 * Updates the Angular module name (including the spec file and index.ts)
 *
 * Again, if the user has deviated from the expected folder
 * structure, they are very much on their own.
 *
 * @param schema The options provided to the schematic
 */
export declare function updateModuleName(schema: Schema): (tree: import("@angular-devkit/schematics/src/tree/interface").Tree, _context: SchematicContext) => Observable<import("@angular-devkit/schematics/src/tree/interface").Tree>;
