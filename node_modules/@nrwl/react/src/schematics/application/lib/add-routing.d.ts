import { Rule, SchematicContext } from '@angular-devkit/schematics';
import { NormalizedSchema } from '../schema';
export declare function addRouting(options: NormalizedSchema, context: SchematicContext): Rule;
